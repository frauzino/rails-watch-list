<h1>Create a bookmark</h1>

<%= simple_form_for([@list, @bookmark]) do |f| %>
  <%= f.input :comment, as: :text %>
  <%= f.association :movie %>
  <%= f.association :list %>
  <%= f.button :submit %>
<% end %>
